requirejs(['jquery','jqueryPulign/jquery.bubble','js/model/groupName'],
function   (jquery,c) {
    //jQuery, canvas and the app/sub module are all
    //loaded and can be used here now.
    $().canvasBubbles();

});

